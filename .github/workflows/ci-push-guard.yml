# name: Push Guard

# on:
#   push:
#     branches:
#       - develop
#       - staging
#       - production

# permissions:
#   contents: read

# concurrency:
#   group: push-guard-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   deny-direct-push:
#     name: Deny direct push
#     runs-on: ubuntu-latest

#     steps:
#       - name: Fail direct pushes to protected branches
#         run: |
#           echo "Direct pushes to ${GITHUB_REF_NAME} are not allowed by project policy."
#           echo "Use pull requests instead."
#           exit 1
